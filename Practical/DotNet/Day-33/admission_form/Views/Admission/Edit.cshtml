@model admission_form.Models.AdmissionModel
@{ ViewData["Title"] = "Edit Admission"; }

<div class="mb-8 flex items-center gap-4">
    <a asp-action="List" class="p-2 rounded-lg hover:bg-blue-100 transition text-gray-600 hover:text-blue-600" title="Back to list">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/></svg>
    </a>
    <div>
        <h1 class="text-4xl font-bold text-white">Edit Application</h1>
        <p class="text-blue-100 mt-1 text-sm">Updating record #@Model.Id — @Model.Name</p>
    </div>
</div>

<div class="max-w-2xl">
    <div class="card p-8">
        <form id="editForm" asp-action="Edit" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data" novalidate>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
                <!-- Name -->
                <div class="sm:col-span-2">
                    <label for="Name" class="block text-sm font-semibold text-slate-700 mb-2">Full Name <span class="text-red-500">*</span></label>
                    <input type="text" id="Name" name="Name" value="@Model.Name"
                           class="w-full rounded-lg px-4 py-3 text-sm"
                           maxlength="100" />
                    <span class="field-error" id="err-Name">Full name is required (max 100 characters).</span>
                </div>

                <!-- Age -->
                <div>
                    <label for="Age" class="block text-sm font-semibold text-slate-700 mb-2">Age <span class="text-red-500">*</span></label>
                    <input type="number" id="Age" name="Age" value="@Model.Age"
                           class="w-full rounded-lg px-4 py-3 text-sm"
                           min="1" max="120" />
                    <span class="field-error" id="err-Age">Age must be between 1 and 120.</span>
                </div>

                <!-- Phone -->
                <div>
                    <label for="PhoneNumber" class="block text-sm font-semibold text-slate-700 mb-2">Phone Number <span class="text-red-500">*</span></label>
                    <input type="text" id="PhoneNumber" name="PhoneNumber" value="@Model.PhoneNumber"
                           class="w-full rounded-lg px-4 py-3 text-sm"
                           maxlength="10" />
                    <span class="field-error" id="err-PhoneNumber">Phone number is required (max 10 characters).</span>
                </div>

                <!-- Email -->
                <div class="sm:col-span-2">
                    <label for="Email" class="block text-sm font-semibold text-slate-700 mb-2">Email Address</label>
                    <input type="email" id="Email" name="Email" value="@Model.Email"
                           class="w-full rounded-lg px-4 py-3 text-sm"
                           maxlength="100" />
                    <span class="field-error" id="err-Email">Enter a valid email address.</span>
                </div>

                <!-- Profile Image -->
                <div class="sm:col-span-2">
                    <label class="block text-sm font-semibold text-slate-700 mb-2">Profile Photo <span class="text-gray-500 font-normal">(JPG or PNG &bull; max 5 MB &bull; leave blank to keep existing)</span></label>
                    @if (Model.ProfileImage != null)
                    {
                        <div class="flex items-center gap-4 mb-4 p-4 rounded-lg bg-blue-50 border border-blue-200">
                            <img src="@Url.Action("GetImage", new { id = Model.Id })" alt="Current photo" class="w-16 h-16 object-cover rounded-lg ring-2 ring-blue-300" />
                            <div>
                                <p class="text-sm font-semibold text-gray-800">Current photo</p>
                                <p class="text-xs text-gray-600">Upload a new file below to replace it</p>
                            </div>
                        </div>
                    }
                    <div class="file-upload-area p-6 text-center relative" id="dropZone">
                        <input type="file" name="profileImage" id="profileImage" accept=".jpg,.jpeg,.png"
                               class="absolute inset-0 w-full h-full opacity-0 cursor-pointer" />
                        <div id="uploadPlaceholder">
                            <svg class="w-10 h-10 text-blue-400 mx-auto mb-2" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5m-13.5-9L12 3m0 0l4.5 4.5M12 3v13.5"/></svg>
                            <p class="text-sm text-gray-700">Click or drag a new photo here</p>
                        </div>
                        <div id="imagePreview" class="hidden">
                            <img id="previewImg" src="#" alt="Preview" class="w-16 h-16 object-cover rounded-lg mx-auto mb-2 ring-4 ring-blue-200" />
                            <p id="previewName" class="text-xs text-gray-600"></p>
                        </div>
                    </div>
                    <span class="field-error" id="err-profileImage">Only JPG/PNG files under 5 MB are allowed.</span>
                </div>
            </div>

            <div class="mt-8 flex items-center gap-4">
                <button type="submit" class="btn-primary">
                    Save Changes
                </button>
                <a asp-action="List" class="text-sm text-gray-600 hover:text-blue-600 transition">Cancel</a>
            </div>

        </form>
    </div>
</div>

@section Scripts {
<script>
(function () {
    function showError(id, msg) {
        const el = document.getElementById('err-' + id);
        const input = document.getElementById(id);
        if (el) { el.textContent = msg || el.textContent; el.classList.add('show'); }
        if (input) input.classList.add('invalid');
    }
    function clearError(id) {
        const el = document.getElementById('err-' + id);
        const input = document.getElementById(id);
        if (el) el.classList.remove('show');
        if (input) input.classList.remove('invalid');
    }
    function validateName() {
        const v = document.getElementById('Name').value.trim();
        if (!v) { showError('Name', 'Full name is required.'); return false; }
        if (v.length > 100) { showError('Name', 'Max 100 characters.'); return false; }
        clearError('Name'); return true;
    }
    function validateAge() {
        const v = parseInt(document.getElementById('Age').value, 10);
        if (isNaN(v) || v < 1 || v > 120) { showError('Age', 'Age must be between 1 and 120.'); return false; }
        clearError('Age'); return true;
    }
    function validatePhone() {
        const v = document.getElementById('PhoneNumber').value.trim();
        if (!v) { showError('PhoneNumber', 'Phone number is required.'); return false; }
        if (v.length > 10) { showError('PhoneNumber', 'Max 10 characters.'); return false; }
        clearError('PhoneNumber'); return true;
    }
    function validateEmail() {
        const v = document.getElementById('Email').value.trim();
        if (v && !/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(v)) { showError('Email', 'Enter a valid email address.'); return false; }
        clearError('Email'); return true;
    }
    function validateImage(file) {
        if (!file) { clearError('profileImage'); return true; }
        if (!['image/jpeg','image/png'].includes(file.type)) { showError('profileImage', 'Only JPG and PNG images are allowed.'); return false; }
        if (file.size > 5 * 1024 * 1024) { showError('profileImage', 'Image must be smaller than 5 MB.'); return false; }
        clearError('profileImage'); return true;
    }
    document.getElementById('Name').addEventListener('blur', validateName);
    document.getElementById('Age').addEventListener('blur', validateAge);
    document.getElementById('PhoneNumber').addEventListener('blur', validatePhone);
    document.getElementById('Email').addEventListener('blur', validateEmail);
    const imgInput = document.getElementById('profileImage');
    imgInput.addEventListener('change', function () {
        const file = this.files[0];
        if (!validateImage(file)) return;
        if (file) {
            const reader = new FileReader();
            reader.onload = e => {
                document.getElementById('previewImg').src = e.target.result;
                document.getElementById('previewName').textContent = file.name;
                document.getElementById('uploadPlaceholder').classList.add('hidden');
                document.getElementById('imagePreview').classList.remove('hidden');
            };
            reader.readAsDataURL(file);
        }
    });
    document.getElementById('editForm').addEventListener('submit', function (e) {
        const file = imgInput.files[0];
        const ok = [validateName(), validateAge(), validatePhone(), validateEmail(), validateImage(file)];
        if (ok.includes(false)) e.preventDefault();
    });
})();
</script>
}
